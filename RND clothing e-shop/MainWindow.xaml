<Window x:Class="RND_clothing_e_shop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RND Clothing"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        ResizeMode="CanResize"
        Background="Black"
        MinWidth="1000"
        MinHeight="600">

    <Grid>
        <MediaElement x:Name="BackgroundVideo"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"
                      Stretch="UniformToFill"
                      Volume="0"
                      IsHitTestVisible="False"
                      Panel.ZIndex="0" />

        <Grid Background="#80000000" Panel.ZIndex="1"/>

        <Viewbox Stretch="Uniform" Panel.ZIndex="2">
            <Grid Width="1400" Height="800">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="1"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Width="500">

                    <TextBlock Text="RND CLOTHING"
                               Foreground="White"
                               FontSize="48"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Margin="0,0,0,12"/>

                    <TextBlock Text="Štýlový obchod s oblečením"
                               Foreground="#EAEAEA"
                               FontSize="20"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Margin="0,0,0,40"/>

                    <Button x:Name="LoginButton"
                            Content="Prihlásiť sa"
                            Height="55"
                            Margin="0,0,0,15"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Background="White"
                            Foreground="Black"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="LoginButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="14">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="RegisterButton"
                            Content="Registrovať sa"
                            Height="55"
                            Margin="0,0,0,15"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Background="#FF1F1F1F"
                            Foreground="White"
                            BorderBrush="White"
                            BorderThickness="2"
                            Cursor="Hand"
                            Click="RegisterButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="14">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="GuestButton"
                            Content="Pokračovať bez prihlásenia"
                            Height="55"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Background="#CCFFFFFF"
                            Foreground="Black"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="GuestButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="14">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Viewbox>
    </Grid>
</Window>